/**
 * Manual override for the featured show displayed in the Hero section.
 *
 * Set this to a show's slug (e.g. "hamlet") to feature that specific show.
 * Set to `null` to use the default behavior (highest-rated show).
 *
 * To change the featured show, update the slug below and deploy.
 * You can find a show's slug in its URL: /shows/<slug>
 */
export const FEATURED_SHOW_SLUG: string | null = "צ׳ילבות";
